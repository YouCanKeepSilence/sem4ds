#ifndef NQUEUE_H
#define NQUEUE_H



class NQueue
{
public:
    NQueue();
//    NQueue();
    NQueue * p_next;
    NQueue * p_first;
//    void AddToQueue(T data);
private:
//    T m_data;
};

#endif // NQUEUE_H
